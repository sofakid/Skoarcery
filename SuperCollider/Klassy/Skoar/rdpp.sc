// ==================================================================================================================================
// rdpp.sc.sc - Generated by Code_Parser_Sc - Create Recursive Descent Predictive Parser on 2014-10-14 07:37:57 for SuperCollider 3.6
// ==================================================================================================================================

SkoarParseException : Exception {

}

SkoarParser {

    var <runtime, <toker, <tab, desirables;

    *new {
        | runtime |
        ^super.new.init( runtime )
    }

    init {
        | runtime |

        runtime = runtime;
        toker = runtime.toker;
        tab = 0;
        desirables = IdentityDictionary();
        this.init_desirables();
    }

    fail {
        toker.dump;
        SkoarParseException("Fail").throw;
    }

    //print {
    //    | line, end |
    //    (line ++ end).postln;
    //}

    init_desirables {

        // accidentally
        desirables.put('accidentally -> acc noaty', [Toke_AccFlat, Toke_AccSharp, Toke_AccNatural]);

        // stmt
        desirables.put('stmt -> optional_carrots skoaroid msg_chain_node', [Toke_OttavaB, Toke_OctaveShift, Toke_QuindicesimaA, Toke_QuindicesimaB, Toke_LWing,
                   Toke_PedalDown, Toke_DynFP, Toke_Portamento, Toke_String, Toke_DubRep,
                   Toke_Symbol, Toke_OttavaA, Toke_CondS, Toke_AccNatural, Toke_Slur,
                   Toke_Caesura, Toke_CurNoat, Toke_AccSharp, Toke_DynPiano, Toke_DynForte,
                   Toke_Int, Toke_Rep, Toke_Tuplet, Toke_Float, Toke_PedalUp,
                   Toke_ListS, Toke_Carrots, Toke_DynSFZ, Toke_Choard, Toke_Loco,
                   Toke_AccFlat, Toke_NamedNoat]);

        // dynamic
        desirables.put('dynamic -> DynPiano', [Toke_DynPiano]);
        desirables.put('dynamic -> DynForte', [Toke_DynForte]);
        desirables.put('dynamic -> DynSFZ', [Toke_DynSFZ]);
        desirables.put('dynamic -> DynFP', [Toke_DynFP]);

        // skoar
        desirables.put('skoar -> branches', [Toke_OttavaB, Toke_OctaveShift, Toke_QuindicesimaA, Toke_EOF, Toke_QuindicesimaB,
                   Toke_LWing, Toke_PedalDown, Toke_Voice, Toke_DynFP, Toke_Portamento,
                   Toke_String, Toke_Comment, Toke_DubRep, Toke_OttavaA, Toke_Symbol,
                   Toke_CondS, Toke_Slash, Toke_Coda, Toke_AccNatural, Toke_Slur,
                   Toke_Segno, Toke_Caesura, Toke_CurNoat, Toke_DynPiano, Toke_AccSharp,
                   Toke_DynForte, Toke_MeterS, Toke_Volta, Toke_Int, Toke_DaCapo,
                   Toke_Eighths, Toke_Rep, Toke_Tuplet, Toke_Float, Toke_PedalUp,
                   Toke_Quavers, Toke_ListS, Toke_Crotchets, Toke_DynSFZ, Toke_Newline,
                   Toke_Choard, Toke_Loco, Toke_Fine, Toke_Bars, Toke_AccFlat,
                   Toke_DalSegno, Toke_NamedNoat, Toke_Quarters]);

        // meter_ass
        desirables.put('meter_ass -> AssOp meter_ass_r', [Toke_AssOp]);

        // moar_listy_entries
        desirables.put('moar_listy_entries -> ListSep listy_entries', [Toke_ListSep]);
        desirables.put('moar_listy_entries -> Newline', [Toke_Newline]);

        // al_x
        desirables.put('al_x -> AlCoda', [Toke_AlCoda]);
        desirables.put('al_x -> AlSegno', [Toke_AlSegno]);
        desirables.put('al_x -> AlFine', [Toke_AlFine]);

        // meter_stmt
        desirables.put('meter_stmt -> Int meter_stmt_numbery', [Toke_Int]);
        desirables.put('meter_stmt -> meteroid', [Toke_Symbol, Toke_TrebleClef, Toke_AltoClef, Toke_BassClef, Toke_Carrots]);

        // acc
        desirables.put('acc -> AccSharp', [Toke_AccSharp]);
        desirables.put('acc -> AccNatural', [Toke_AccNatural]);
        desirables.put('acc -> AccFlat', [Toke_AccFlat]);

        // branch
        desirables.put('branch -> optional_voice phrases Newline', [Toke_OttavaB, Toke_OctaveShift, Toke_QuindicesimaA, Toke_QuindicesimaB, Toke_LWing,
                   Toke_PedalDown, Toke_Voice, Toke_DynFP, Toke_Portamento, Toke_String,
                   Toke_Comment, Toke_DubRep, Toke_OttavaA, Toke_Symbol, Toke_CondS,
                   Toke_Slash, Toke_Coda, Toke_AccNatural, Toke_Slur, Toke_Segno,
                   Toke_Caesura, Toke_CurNoat, Toke_DynPiano, Toke_AccSharp, Toke_DynForte,
                   Toke_MeterS, Toke_Volta, Toke_Int, Toke_DaCapo, Toke_Eighths,
                   Toke_Rep, Toke_Tuplet, Toke_Float, Toke_PedalUp, Toke_Quavers,
                   Toke_ListS, Toke_Crotchets, Toke_DynSFZ, Toke_Newline, Toke_Choard,
                   Toke_Loco, Toke_Fine, Toke_Bars, Toke_AccFlat, Toke_DalSegno,
                   Toke_NamedNoat, Toke_Quarters]);

        // ottavas
        desirables.put('ottavas -> OctaveShift', [Toke_OctaveShift]);
        desirables.put('ottavas -> OttavaA', [Toke_OttavaA]);
        desirables.put('ottavas -> OttavaB', [Toke_OttavaB]);
        desirables.put('ottavas -> QuindicesimaA', [Toke_QuindicesimaA]);
        desirables.put('ottavas -> QuindicesimaB', [Toke_QuindicesimaB]);
        desirables.put('ottavas -> Loco', [Toke_Loco]);

        // beat
        desirables.put('beat -> Crotchets', [Toke_Crotchets]);
        desirables.put('beat -> Quavers', [Toke_Quavers]);
        desirables.put('beat -> Quarters', [Toke_Quarters]);
        desirables.put('beat -> Eighths', [Toke_Eighths]);
        desirables.put('beat -> Slash', [Toke_Slash]);

        // conditional
        desirables.put('conditional -> CondS optional_stmt CondSep boolean CondSep optional_stmt CondE', [Toke_CondS]);

        // optional_soak
        desirables.put('optional_soak -> Soak', [Toke_Soak]);

        // dal_goto
        desirables.put('dal_goto -> DaCapo al_x', [Toke_DaCapo]);
        desirables.put('dal_goto -> DalSegno al_x', [Toke_DalSegno]);

        // musical_keyword_misc
        desirables.put('musical_keyword_misc -> Rep', [Toke_Rep]);
        desirables.put('musical_keyword_misc -> DubRep', [Toke_DubRep]);
        desirables.put('musical_keyword_misc -> Portamento', [Toke_Portamento]);

        // skoaroid
        desirables.put('skoaroid -> nouny skoaroid_prime', [Toke_OttavaB, Toke_OctaveShift, Toke_AccNatural, Toke_Slur, Toke_QuindicesimaA,
                   Toke_Caesura, Toke_CurNoat, Toke_AccSharp, Toke_DynPiano, Toke_DynForte,
                   Toke_QuindicesimaB, Toke_LWing, Toke_Int, Toke_PedalDown, Toke_PedalUp,
                   Toke_DynFP, Toke_Tuplet, Toke_Float, Toke_Portamento, Toke_Rep,
                   Toke_ListS, Toke_String, Toke_DynSFZ, Toke_DubRep, Toke_Symbol,
                   Toke_OttavaA, Toke_Choard, Toke_Loco, Toke_CondS, Toke_AccFlat,
                   Toke_NamedNoat]);

        // boolean
        desirables.put('boolean -> skoaroid BooleanOp skoaroid', [Toke_OttavaB, Toke_OctaveShift, Toke_AccNatural, Toke_Slur, Toke_QuindicesimaA,
                   Toke_Caesura, Toke_CurNoat, Toke_AccSharp, Toke_DynPiano, Toke_DynForte,
                   Toke_QuindicesimaB, Toke_LWing, Toke_Int, Toke_PedalDown, Toke_Rep,
                   Toke_DynFP, Toke_Tuplet, Toke_Float, Toke_Portamento, Toke_PedalUp,
                   Toke_ListS, Toke_String, Toke_DynSFZ, Toke_DubRep, Toke_Symbol,
                   Toke_OttavaA, Toke_Choard, Toke_Loco, Toke_CondS, Toke_AccFlat,
                   Toke_NamedNoat]);

        // noaty
        desirables.put('noaty -> noat_literal', [Toke_Choard, Toke_NamedNoat]);
        desirables.put('noaty -> noat_reference', [Toke_Symbol, Toke_CurNoat, Toke_ListS]);

        // phrasey
        desirables.put('phrasey -> Comment', [Toke_Comment]);
        desirables.put('phrasey -> marker', [Toke_Segno, Toke_Fine, Toke_Volta, Toke_Bars, Toke_Coda]);
        desirables.put('phrasey -> meter', [Toke_MeterS]);
        desirables.put('phrasey -> skoaroid', [Toke_OttavaB, Toke_OctaveShift, Toke_AccNatural, Toke_Slur, Toke_QuindicesimaA,
                   Toke_Caesura, Toke_CurNoat, Toke_AccSharp, Toke_DynPiano, Toke_DynForte,
                   Toke_QuindicesimaB, Toke_LWing, Toke_Int, Toke_PedalDown, Toke_Rep,
                   Toke_DynFP, Toke_Tuplet, Toke_Float, Toke_Portamento, Toke_PedalUp,
                   Toke_ListS, Toke_String, Toke_DynSFZ, Toke_DubRep, Toke_Symbol,
                   Toke_OttavaA, Toke_Choard, Toke_Loco, Toke_CondS, Toke_AccFlat,
                   Toke_NamedNoat]);
        desirables.put('phrasey -> dal_goto', [Toke_DaCapo, Toke_DalSegno]);
        desirables.put('phrasey -> beat', [Toke_Crotchets, Toke_Slash, Toke_Quavers, Toke_Eighths, Toke_Quarters]);

        // nouny_literal
        desirables.put('nouny_literal -> Tuplet', [Toke_Tuplet]);
        desirables.put('nouny_literal -> Caesura', [Toke_Caesura]);
        desirables.put('nouny_literal -> Slur', [Toke_Slur]);
        desirables.put('nouny_literal -> Int', [Toke_Int]);
        desirables.put('nouny_literal -> Float', [Toke_Float]);
        desirables.put('nouny_literal -> String', [Toke_String]);

        // cthulhu
        desirables.put('cthulhu -> LWing CondSep cthulhu_prime', [Toke_LWing]);

        // noat_reference
        desirables.put('noat_reference -> Symbol', [Toke_Symbol]);
        desirables.put('noat_reference -> CurNoat', [Toke_CurNoat]);
        desirables.put('noat_reference -> listy', [Toke_ListS]);

        // optional_voice
        desirables.put('optional_voice -> Voice', [Toke_Voice]);

        // coda
        desirables.put('coda -> Coda optional_al_coda', [Toke_Coda]);

        // settable
        desirables.put('settable -> Caesura', [Toke_Caesura]);
        desirables.put('settable -> CurNoat', [Toke_CurNoat]);
        desirables.put('settable -> Symbol', [Toke_Symbol]);
        desirables.put('settable -> listy', [Toke_ListS]);

        // meter
        desirables.put('meter -> MeterS meter_stmts MeterE', [Toke_MeterS]);

        // optional_carrots
        desirables.put('optional_carrots -> Carrots', [Toke_Carrots]);

        // skoaroid_prime
        desirables.put('skoaroid_prime -> assignment skoaroid_prime', [Toke_AssOp]);

        // branches
        desirables.put('branches -> branch branches', [Toke_OttavaB, Toke_OctaveShift, Toke_QuindicesimaA, Toke_QuindicesimaB, Toke_LWing,
                   Toke_PedalDown, Toke_Voice, Toke_DynFP, Toke_Portamento, Toke_String,
                   Toke_Comment, Toke_DubRep, Toke_OttavaA, Toke_Symbol, Toke_CondS,
                   Toke_Slash, Toke_Coda, Toke_AccNatural, Toke_Slur, Toke_Segno,
                   Toke_Caesura, Toke_CurNoat, Toke_DynPiano, Toke_AccSharp, Toke_DynForte,
                   Toke_MeterS, Toke_Volta, Toke_Int, Toke_DaCapo, Toke_Eighths,
                   Toke_Rep, Toke_Tuplet, Toke_Float, Toke_PedalUp, Toke_Quavers,
                   Toke_ListS, Toke_Crotchets, Toke_DynSFZ, Toke_Newline, Toke_Choard,
                   Toke_Loco, Toke_Fine, Toke_Bars, Toke_AccFlat, Toke_DalSegno,
                   Toke_NamedNoat, Toke_Quarters]);

        // meter_symbolic
        desirables.put('meter_symbolic -> optional_carrots Symbol msg_chain_node', [Toke_Symbol, Toke_Carrots]);

        // listy_entries
        desirables.put('listy_entries -> skoaroid moar_listy_entries', [Toke_OttavaB, Toke_OctaveShift, Toke_AccNatural, Toke_Slur, Toke_QuindicesimaA,
                   Toke_Caesura, Toke_CurNoat, Toke_AccSharp, Toke_DynPiano, Toke_DynForte,
                   Toke_QuindicesimaB, Toke_LWing, Toke_Int, Toke_PedalDown, Toke_Rep,
                   Toke_DynFP, Toke_Tuplet, Toke_Float, Toke_Portamento, Toke_PedalUp,
                   Toke_ListS, Toke_String, Toke_DynSFZ, Toke_DubRep, Toke_Symbol,
                   Toke_OttavaA, Toke_Choard, Toke_Loco, Toke_CondS, Toke_AccFlat,
                   Toke_NamedNoat]);

        // pedally
        desirables.put('pedally -> PedalDown', [Toke_PedalDown]);
        desirables.put('pedally -> PedalUp', [Toke_PedalUp]);

        // meter_stmts
        desirables.put('meter_stmts -> meter_stmt meter_stmts', [Toke_TrebleClef, Toke_Symbol, Toke_AltoClef, Toke_BassClef, Toke_Int,
                   Toke_Carrots]);
        desirables.put('meter_stmts -> Newline', [Toke_Newline]);

        // meter_beat
        desirables.put('meter_beat -> Crotchets', [Toke_Crotchets]);
        desirables.put('meter_beat -> Quavers', [Toke_Quavers]);
        desirables.put('meter_beat -> Quarters', [Toke_Quarters]);
        desirables.put('meter_beat -> Eighths', [Toke_Eighths]);
        desirables.put('meter_beat -> Slash', [Toke_Slash]);

        // optional_al_coda
        desirables.put('optional_al_coda -> AlCoda', [Toke_AlCoda]);

        // clef
        desirables.put('clef -> TrebleClef', [Toke_TrebleClef]);
        desirables.put('clef -> BassClef', [Toke_BassClef]);
        desirables.put('clef -> AltoClef', [Toke_AltoClef]);

        // msg
        desirables.put('msg -> MsgNameWithArgs listy_suffix', [Toke_MsgNameWithArgs]);
        desirables.put('msg -> MsgName', [Toke_MsgName]);
        desirables.put('msg -> listy', [Toke_ListS]);

        // msg_chain_node
        desirables.put('msg_chain_node -> optional_soak MsgOp msg msg_chain_node', [Toke_MsgOp, Toke_Soak]);

        // cthulhu_prime
        desirables.put('cthulhu_prime -> boolean CondSep RWing', [Toke_OttavaB, Toke_OctaveShift, Toke_AccNatural, Toke_Slur, Toke_QuindicesimaA,
                   Toke_Caesura, Toke_CurNoat, Toke_AccSharp, Toke_DynPiano, Toke_DynForte,
                   Toke_QuindicesimaB, Toke_LWing, Toke_Int, Toke_PedalDown, Toke_Rep,
                   Toke_DynFP, Toke_Tuplet, Toke_Float, Toke_Portamento, Toke_PedalUp,
                   Toke_ListS, Toke_String, Toke_DynSFZ, Toke_DubRep, Toke_Symbol,
                   Toke_OttavaA, Toke_Choard, Toke_Loco, Toke_CondS, Toke_AccFlat,
                   Toke_NamedNoat]);
        desirables.put('cthulhu_prime -> Nosey CondSep RWing', [Toke_Nosey]);

        // assignment
        desirables.put('assignment -> AssOp settable', [Toke_AssOp]);

        // listy
        desirables.put('listy -> ListS listy_suffix', [Toke_ListS]);

        // listy_suffix
        desirables.put('listy_suffix -> listy_entries ListE', [Toke_OttavaB, Toke_OctaveShift, Toke_AccNatural, Toke_Slur, Toke_QuindicesimaA,
                   Toke_Caesura, Toke_CurNoat, Toke_AccSharp, Toke_DynPiano, Toke_DynForte,
                   Toke_QuindicesimaB, Toke_LWing, Toke_Int, Toke_PedalDown, Toke_Rep,
                   Toke_DynFP, Toke_Tuplet, Toke_Float, Toke_Portamento, Toke_PedalUp,
                   Toke_ListS, Toke_String, Toke_DynSFZ, Toke_DubRep, Toke_Symbol,
                   Toke_OttavaA, Toke_Choard, Toke_Loco, Toke_CondS, Toke_AccFlat,
                   Toke_NamedNoat]);

        // meter_stmt_numbery
        desirables.put('meter_stmt_numbery -> meter_ass', [Toke_AssOp]);
        desirables.put('meter_stmt_numbery -> meter_sig_prime', [Toke_Slash]);

        // meteroid
        desirables.put('meteroid -> meter_symbolic', [Toke_Symbol, Toke_Carrots]);
        desirables.put('meteroid -> clef', [Toke_TrebleClef, Toke_AltoClef, Toke_BassClef]);

        // nouny
        desirables.put('nouny -> cthulhu', [Toke_LWing]);
        desirables.put('nouny -> noaty', [Toke_Choard, Toke_Symbol, Toke_ListS, Toke_NamedNoat, Toke_CurNoat]);
        desirables.put('nouny -> conditional', [Toke_CondS]);
        desirables.put('nouny -> nouny_literal', [Toke_Float, Toke_Int, Toke_Slur, Toke_Caesura, Toke_String,
                   Toke_Tuplet]);
        desirables.put('nouny -> accidentally', [Toke_AccFlat, Toke_AccNatural, Toke_AccSharp]);
        desirables.put('nouny -> musical_keyword', [Toke_PedalUp, Toke_Portamento, Toke_OttavaB, Toke_OctaveShift, Toke_QuindicesimaA,
                   Toke_DynSFZ, Toke_DynPiano, Toke_DubRep, Toke_DynForte, Toke_OttavaA,
                   Toke_Loco, Toke_QuindicesimaB, Toke_PedalDown, Toke_Rep, Toke_DynFP]);

        // marker
        desirables.put('marker -> Segno', [Toke_Segno]);
        desirables.put('marker -> Fine', [Toke_Fine]);
        desirables.put('marker -> coda', [Toke_Coda]);
        desirables.put('marker -> Volta', [Toke_Volta]);
        desirables.put('marker -> Bars', [Toke_Bars]);

        // optional_stmt
        desirables.put('optional_stmt -> stmt', [Toke_OttavaB, Toke_OctaveShift, Toke_QuindicesimaA, Toke_QuindicesimaB, Toke_LWing,
                   Toke_PedalDown, Toke_DynFP, Toke_Portamento, Toke_String, Toke_DubRep,
                   Toke_Symbol, Toke_OttavaA, Toke_CondS, Toke_AccNatural, Toke_Slur,
                   Toke_Caesura, Toke_CurNoat, Toke_AccSharp, Toke_DynPiano, Toke_DynForte,
                   Toke_Int, Toke_Rep, Toke_Tuplet, Toke_Float, Toke_PedalUp,
                   Toke_ListS, Toke_Carrots, Toke_DynSFZ, Toke_Choard, Toke_Loco,
                   Toke_AccFlat, Toke_NamedNoat]);
        desirables.put('optional_stmt -> Newline', [Toke_Newline]);

        // phrases
        desirables.put('phrases -> phrasey phrases', [Toke_OttavaB, Toke_OctaveShift, Toke_QuindicesimaA, Toke_QuindicesimaB, Toke_LWing,
                   Toke_PedalDown, Toke_DynFP, Toke_Portamento, Toke_String, Toke_Comment,
                   Toke_DubRep, Toke_Symbol, Toke_OttavaA, Toke_CondS, Toke_Slash,
                   Toke_Coda, Toke_AccNatural, Toke_Slur, Toke_Segno, Toke_Caesura,
                   Toke_CurNoat, Toke_AccSharp, Toke_DynPiano, Toke_DynForte, Toke_MeterS,
                   Toke_Volta, Toke_Int, Toke_DaCapo, Toke_Eighths, Toke_Rep,
                   Toke_Tuplet, Toke_Float, Toke_PedalUp, Toke_Quavers, Toke_ListS,
                   Toke_Crotchets, Toke_DynSFZ, Toke_Choard, Toke_Loco, Toke_Fine,
                   Toke_Bars, Toke_AccFlat, Toke_DalSegno, Toke_NamedNoat, Toke_Quarters]);

        // noat_literal
        desirables.put('noat_literal -> Choard', [Toke_Choard]);
        desirables.put('noat_literal -> NamedNoat', [Toke_NamedNoat]);

        // musical_keyword
        desirables.put('musical_keyword -> dynamic', [Toke_DynFP, Toke_DynForte, Toke_DynSFZ, Toke_DynPiano]);
        desirables.put('musical_keyword -> ottavas', [Toke_OttavaA, Toke_Loco, Toke_QuindicesimaB, Toke_OttavaB, Toke_OctaveShift,
                   Toke_QuindicesimaA]);
        desirables.put('musical_keyword -> pedally', [Toke_PedalDown, Toke_PedalUp]);
        desirables.put('musical_keyword -> musical_keyword_misc', [Toke_DubRep, Toke_Portamento, Toke_Rep]);

        // meter_ass_r
        desirables.put('meter_ass_r -> Symbol', [Toke_Symbol]);
        desirables.put('meter_ass_r -> meter_beat', [Toke_Crotchets, Toke_Slash, Toke_Quavers, Toke_Eighths, Toke_Quarters]);
        desirables.put('meter_ass_r -> dynamic', [Toke_DynFP, Toke_DynForte, Toke_DynSFZ, Toke_DynPiano]);

        // meter_sig_prime
        desirables.put('meter_sig_prime -> Slash Int', [Toke_Slash]);
    }

    accidentally {
        | parent |

        var noad = SkoarNoad.new("accidentally", nil, parent);
        var desires = nil;

        desires = desirables['accidentally -> acc noaty'];
        // accidentally -> acc noaty
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.acc(noad));
            noad.add_noad(this.noaty(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    stmt {
        | parent |

        var noad = SkoarNoad.new("stmt", nil, parent);
        var desires = nil;

        desires = desirables['stmt -> optional_carrots skoaroid msg_chain_node'];
        // stmt -> optional_carrots skoaroid msg_chain_node
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.optional_carrots(noad));
            noad.add_noad(this.skoaroid(noad));
            noad.add_noad(this.msg_chain_node(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    dynamic {
        | parent |

        var noad = SkoarNoad.new("dynamic", nil, parent);
        var desires = nil;

        desires = desirables['dynamic -> DynPiano'];
        // dynamic -> DynPiano
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_DynPiano", toker.burn(Toke_DynPiano));
            ^noad;
        };

        desires = desirables['dynamic -> DynForte'];
        // dynamic -> DynForte
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_DynForte", toker.burn(Toke_DynForte));
            ^noad;
        };

        desires = desirables['dynamic -> DynSFZ'];
        // dynamic -> DynSFZ
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_DynSFZ", toker.burn(Toke_DynSFZ));
            ^noad;
        };

        desires = desirables['dynamic -> DynFP'];
        // dynamic -> DynFP
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_DynFP", toker.burn(Toke_DynFP));
            ^noad;
        };

        // Error State
        this.fail;
    }

    skoar {
        | parent |

        var noad = SkoarNoad.new("skoar", nil, parent);
        var desires = nil;

        desires = desirables['skoar -> branches'];
        // skoar -> branches
        if (toker.sees(desires) != nil) {
            this.branches(noad);
            ^noad;
        };

        // Error State
        this.fail;
    }

    meter_ass {
        | parent |

        var noad = SkoarNoad.new("meter_ass", nil, parent);
        var desires = nil;

        desires = desirables['meter_ass -> AssOp meter_ass_r'];
        // meter_ass -> AssOp meter_ass_r
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_AssOp", toker.burn(Toke_AssOp));
            this.meter_ass_r(noad);
            ^noad;
        };

        // Error State
        this.fail;
    }

    moar_listy_entries {
        | parent |

        var noad = parent;
        var desires = nil;

        desires = desirables['moar_listy_entries -> ListSep listy_entries'];
        // moar_listy_entries -> ListSep listy_entries
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_ListSep", toker.burn(Toke_ListSep));
            this.listy_entries(noad);
            ^noad;
        };

        desires = desirables['moar_listy_entries -> Newline'];
        // moar_listy_entries -> Newline
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Newline", toker.burn(Toke_Newline));
            ^noad;
        };

        // <e>
        ^noad;
    }

    al_x {
        | parent |

        var noad = SkoarNoad.new("al_x", nil, parent);
        var desires = nil;

        desires = desirables['al_x -> AlCoda'];
        // al_x -> AlCoda
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_AlCoda", toker.burn(Toke_AlCoda));
            ^noad;
        };

        desires = desirables['al_x -> AlSegno'];
        // al_x -> AlSegno
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_AlSegno", toker.burn(Toke_AlSegno));
            ^noad;
        };

        desires = desirables['al_x -> AlFine'];
        // al_x -> AlFine
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_AlFine", toker.burn(Toke_AlFine));
            ^noad;
        };

        // <e>
        ^noad;
    }

    meter_stmt {
        | parent |

        var noad = SkoarNoad.new("meter_stmt", nil, parent);
        var desires = nil;

        desires = desirables['meter_stmt -> Int meter_stmt_numbery'];
        // meter_stmt -> Int meter_stmt_numbery
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Int", toker.burn(Toke_Int));
            this.meter_stmt_numbery(noad);
            ^noad;
        };

        desires = desirables['meter_stmt -> meteroid'];
        // meter_stmt -> meteroid
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.meteroid(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    acc {
        | parent |

        var noad = SkoarNoad.new("acc", nil, parent);
        var desires = nil;

        desires = desirables['acc -> AccSharp'];
        // acc -> AccSharp
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_AccSharp", toker.burn(Toke_AccSharp));
            ^noad;
        };

        desires = desirables['acc -> AccNatural'];
        // acc -> AccNatural
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_AccNatural", toker.burn(Toke_AccNatural));
            ^noad;
        };

        desires = desirables['acc -> AccFlat'];
        // acc -> AccFlat
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_AccFlat", toker.burn(Toke_AccFlat));
            ^noad;
        };

        // Error State
        this.fail;
    }

    branch {
        | parent |

        var noad = SkoarNoad.new("branch", nil, parent);
        var desires = nil;

        desires = desirables['branch -> optional_voice phrases Newline'];
        // branch -> optional_voice phrases Newline
        if (toker.sees(desires) != nil) {
            this.optional_voice(noad);
            this.phrases(noad);
            noad.add_toke("Toke_Newline", toker.burn(Toke_Newline));
            ^noad;
        };

        // Error State
        this.fail;
    }

    ottavas {
        | parent |

        var noad = SkoarNoad.new("ottavas", nil, parent);
        var desires = nil;

        desires = desirables['ottavas -> OctaveShift'];
        // ottavas -> OctaveShift
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_OctaveShift", toker.burn(Toke_OctaveShift));
            ^noad;
        };

        desires = desirables['ottavas -> OttavaA'];
        // ottavas -> OttavaA
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_OttavaA", toker.burn(Toke_OttavaA));
            ^noad;
        };

        desires = desirables['ottavas -> OttavaB'];
        // ottavas -> OttavaB
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_OttavaB", toker.burn(Toke_OttavaB));
            ^noad;
        };

        desires = desirables['ottavas -> QuindicesimaA'];
        // ottavas -> QuindicesimaA
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_QuindicesimaA", toker.burn(Toke_QuindicesimaA));
            ^noad;
        };

        desires = desirables['ottavas -> QuindicesimaB'];
        // ottavas -> QuindicesimaB
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_QuindicesimaB", toker.burn(Toke_QuindicesimaB));
            ^noad;
        };

        desires = desirables['ottavas -> Loco'];
        // ottavas -> Loco
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Loco", toker.burn(Toke_Loco));
            ^noad;
        };

        // Error State
        this.fail;
    }

    beat {
        | parent |

        var noad = SkoarNoad.new("beat", nil, parent);
        var desires = nil;

        desires = desirables['beat -> Crotchets'];
        // beat -> Crotchets
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Crotchets", toker.burn(Toke_Crotchets));
            ^noad;
        };

        desires = desirables['beat -> Quavers'];
        // beat -> Quavers
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Quavers", toker.burn(Toke_Quavers));
            ^noad;
        };

        desires = desirables['beat -> Quarters'];
        // beat -> Quarters
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Quarters", toker.burn(Toke_Quarters));
            ^noad;
        };

        desires = desirables['beat -> Eighths'];
        // beat -> Eighths
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Eighths", toker.burn(Toke_Eighths));
            ^noad;
        };

        desires = desirables['beat -> Slash'];
        // beat -> Slash
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Slash", toker.burn(Toke_Slash));
            ^noad;
        };

        // Error State
        this.fail;
    }

    conditional {
        | parent |

        var noad = SkoarNoad.new("conditional", nil, parent);
        var desires = nil;

        desires = desirables['conditional -> CondS optional_stmt CondSep boolean CondSep optional_stmt CondE'];
        // conditional -> CondS optional_stmt CondSep boolean CondSep optional_stmt CondE
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_CondS", toker.burn(Toke_CondS));
            noad.add_noad(this.optional_stmt(noad));
            noad.add_toke("Toke_CondSep", toker.burn(Toke_CondSep));
            noad.add_noad(this.boolean(noad));
            noad.add_toke("Toke_CondSep", toker.burn(Toke_CondSep));
            noad.add_noad(this.optional_stmt(noad));
            noad.add_toke("Toke_CondE", toker.burn(Toke_CondE));
            ^noad;
        };

        // Error State
        this.fail;
    }

    optional_soak {
        | parent |

        var noad = SkoarNoad.new("optional_soak", nil, parent);
        var desires = nil;

        desires = desirables['optional_soak -> Soak'];
        // optional_soak -> Soak
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Soak", toker.burn(Toke_Soak));
            ^noad;
        };

        // <e>
        ^noad;
    }

    dal_goto {
        | parent |

        var noad = SkoarNoad.new("dal_goto", nil, parent);
        var desires = nil;

        desires = desirables['dal_goto -> DaCapo al_x'];
        // dal_goto -> DaCapo al_x
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_DaCapo", toker.burn(Toke_DaCapo));
            noad.add_noad(this.al_x(noad));
            ^noad;
        };

        desires = desirables['dal_goto -> DalSegno al_x'];
        // dal_goto -> DalSegno al_x
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_DalSegno", toker.burn(Toke_DalSegno));
            noad.add_noad(this.al_x(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    musical_keyword_misc {
        | parent |

        var noad = SkoarNoad.new("musical_keyword_misc", nil, parent);
        var desires = nil;

        desires = desirables['musical_keyword_misc -> Rep'];
        // musical_keyword_misc -> Rep
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Rep", toker.burn(Toke_Rep));
            ^noad;
        };

        desires = desirables['musical_keyword_misc -> DubRep'];
        // musical_keyword_misc -> DubRep
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_DubRep", toker.burn(Toke_DubRep));
            ^noad;
        };

        desires = desirables['musical_keyword_misc -> Portamento'];
        // musical_keyword_misc -> Portamento
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Portamento", toker.burn(Toke_Portamento));
            ^noad;
        };

        // Error State
        this.fail;
    }

    skoaroid {
        | parent |

        var noad = SkoarNoad.new("skoaroid", nil, parent);
        var desires = nil;

        desires = desirables['skoaroid -> nouny skoaroid_prime'];
        // skoaroid -> nouny skoaroid_prime
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.nouny(noad));
            this.skoaroid_prime(noad);
            ^noad;
        };

        // Error State
        this.fail;
    }

    boolean {
        | parent |

        var noad = SkoarNoad.new("boolean", nil, parent);
        var desires = nil;

        desires = desirables['boolean -> skoaroid BooleanOp skoaroid'];
        // boolean -> skoaroid BooleanOp skoaroid
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.skoaroid(noad));
            noad.add_toke("Toke_BooleanOp", toker.burn(Toke_BooleanOp));
            noad.add_noad(this.skoaroid(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    noaty {
        | parent |

        var noad = SkoarNoad.new("noaty", nil, parent);
        var desires = nil;

        desires = desirables['noaty -> noat_literal'];
        // noaty -> noat_literal
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.noat_literal(noad));
            ^noad;
        };

        desires = desirables['noaty -> noat_reference'];
        // noaty -> noat_reference
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.noat_reference(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    phrasey {
        | parent |

        var noad = parent;
        var desires = nil;

        desires = desirables['phrasey -> Comment'];
        // phrasey -> Comment
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Comment", toker.burn(Toke_Comment));
            ^noad;
        };

        desires = desirables['phrasey -> marker'];
        // phrasey -> marker
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.marker(noad));
            ^noad;
        };

        desires = desirables['phrasey -> meter'];
        // phrasey -> meter
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.meter(noad));
            ^noad;
        };

        desires = desirables['phrasey -> skoaroid'];
        // phrasey -> skoaroid
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.skoaroid(noad));
            ^noad;
        };

        desires = desirables['phrasey -> dal_goto'];
        // phrasey -> dal_goto
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.dal_goto(noad));
            ^noad;
        };

        desires = desirables['phrasey -> beat'];
        // phrasey -> beat
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.beat(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    nouny_literal {
        | parent |

        var noad = parent;
        var desires = nil;

        desires = desirables['nouny_literal -> Tuplet'];
        // nouny_literal -> Tuplet
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Tuplet", toker.burn(Toke_Tuplet));
            ^noad;
        };

        desires = desirables['nouny_literal -> Caesura'];
        // nouny_literal -> Caesura
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Caesura", toker.burn(Toke_Caesura));
            ^noad;
        };

        desires = desirables['nouny_literal -> Slur'];
        // nouny_literal -> Slur
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Slur", toker.burn(Toke_Slur));
            ^noad;
        };

        desires = desirables['nouny_literal -> Int'];
        // nouny_literal -> Int
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Int", toker.burn(Toke_Int));
            ^noad;
        };

        desires = desirables['nouny_literal -> Float'];
        // nouny_literal -> Float
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Float", toker.burn(Toke_Float));
            ^noad;
        };

        desires = desirables['nouny_literal -> String'];
        // nouny_literal -> String
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_String", toker.burn(Toke_String));
            ^noad;
        };

        // Error State
        this.fail;
    }

    cthulhu {
        | parent |

        var noad = SkoarNoad.new("cthulhu", nil, parent);
        var desires = nil;

        desires = desirables['cthulhu -> LWing CondSep cthulhu_prime'];
        // cthulhu -> LWing CondSep cthulhu_prime
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_LWing", toker.burn(Toke_LWing));
            noad.add_toke("Toke_CondSep", toker.burn(Toke_CondSep));
            this.cthulhu_prime(noad);
            ^noad;
        };

        // Error State
        this.fail;
    }

    noat_reference {
        | parent |

        var noad = SkoarNoad.new("noat_reference", nil, parent);
        var desires = nil;

        desires = desirables['noat_reference -> Symbol'];
        // noat_reference -> Symbol
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Symbol", toker.burn(Toke_Symbol));
            ^noad;
        };

        desires = desirables['noat_reference -> CurNoat'];
        // noat_reference -> CurNoat
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_CurNoat", toker.burn(Toke_CurNoat));
            ^noad;
        };

        desires = desirables['noat_reference -> listy'];
        // noat_reference -> listy
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.listy(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    optional_voice {
        | parent |

        var noad = parent;
        var desires = nil;

        desires = desirables['optional_voice -> Voice'];
        // optional_voice -> Voice
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Voice", toker.burn(Toke_Voice));
            ^noad;
        };

        // <e>
        ^noad;
    }

    coda {
        | parent |

        var noad = SkoarNoad.new("coda", nil, parent);
        var desires = nil;

        desires = desirables['coda -> Coda optional_al_coda'];
        // coda -> Coda optional_al_coda
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Coda", toker.burn(Toke_Coda));
            noad.add_noad(this.optional_al_coda(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    settable {
        | parent |

        var noad = parent;
        var desires = nil;

        desires = desirables['settable -> Caesura'];
        // settable -> Caesura
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Caesura", toker.burn(Toke_Caesura));
            ^noad;
        };

        desires = desirables['settable -> CurNoat'];
        // settable -> CurNoat
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_CurNoat", toker.burn(Toke_CurNoat));
            ^noad;
        };

        desires = desirables['settable -> Symbol'];
        // settable -> Symbol
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Symbol", toker.burn(Toke_Symbol));
            ^noad;
        };

        desires = desirables['settable -> listy'];
        // settable -> listy
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.listy(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    meter {
        | parent |

        var noad = SkoarNoad.new("meter", nil, parent);
        var desires = nil;

        desires = desirables['meter -> MeterS meter_stmts MeterE'];
        // meter -> MeterS meter_stmts MeterE
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_MeterS", toker.burn(Toke_MeterS));
            this.meter_stmts(noad);
            noad.add_toke("Toke_MeterE", toker.burn(Toke_MeterE));
            ^noad;
        };

        // Error State
        this.fail;
    }

    optional_carrots {
        | parent |

        var noad = SkoarNoad.new("optional_carrots", nil, parent);
        var desires = nil;

        desires = desirables['optional_carrots -> Carrots'];
        // optional_carrots -> Carrots
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Carrots", toker.burn(Toke_Carrots));
            ^noad;
        };

        // <e>
        ^noad;
    }

    skoaroid_prime {
        | parent |

        var noad = parent;
        var desires = nil;

        desires = desirables['skoaroid_prime -> assignment skoaroid_prime'];
        // skoaroid_prime -> assignment skoaroid_prime
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.assignment(noad));
            this.skoaroid_prime(noad);
            ^noad;
        };

        // <e>
        ^noad;
    }

    branches {
        | parent |

        var noad = parent;
        var desires = nil;

        desires = desirables['branches -> branch branches'];
        // branches -> branch branches
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.branch(noad));
            this.branches(noad);
            ^noad;
        };

        // <e>
        ^noad;
    }

    meter_symbolic {
        | parent |

        var noad = SkoarNoad.new("meter_symbolic", nil, parent);
        var desires = nil;

        desires = desirables['meter_symbolic -> optional_carrots Symbol msg_chain_node'];
        // meter_symbolic -> optional_carrots Symbol msg_chain_node
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.optional_carrots(noad));
            noad.add_toke("Toke_Symbol", toker.burn(Toke_Symbol));
            noad.add_noad(this.msg_chain_node(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    listy_entries {
        | parent |

        var noad = parent;
        var desires = nil;

        desires = desirables['listy_entries -> skoaroid moar_listy_entries'];
        // listy_entries -> skoaroid moar_listy_entries
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.skoaroid(noad));
            this.moar_listy_entries(noad);
            ^noad;
        };

        // Error State
        this.fail;
    }

    pedally {
        | parent |

        var noad = SkoarNoad.new("pedally", nil, parent);
        var desires = nil;

        desires = desirables['pedally -> PedalDown'];
        // pedally -> PedalDown
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_PedalDown", toker.burn(Toke_PedalDown));
            ^noad;
        };

        desires = desirables['pedally -> PedalUp'];
        // pedally -> PedalUp
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_PedalUp", toker.burn(Toke_PedalUp));
            ^noad;
        };

        // Error State
        this.fail;
    }

    meter_stmts {
        | parent |

        var noad = parent;
        var desires = nil;

        desires = desirables['meter_stmts -> meter_stmt meter_stmts'];
        // meter_stmts -> meter_stmt meter_stmts
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.meter_stmt(noad));
            this.meter_stmts(noad);
            ^noad;
        };

        desires = desirables['meter_stmts -> Newline'];
        // meter_stmts -> Newline
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Newline", toker.burn(Toke_Newline));
            ^noad;
        };

        // <e>
        ^noad;
    }

    meter_beat {
        | parent |

        var noad = SkoarNoad.new("meter_beat", nil, parent);
        var desires = nil;

        desires = desirables['meter_beat -> Crotchets'];
        // meter_beat -> Crotchets
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Crotchets", toker.burn(Toke_Crotchets));
            ^noad;
        };

        desires = desirables['meter_beat -> Quavers'];
        // meter_beat -> Quavers
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Quavers", toker.burn(Toke_Quavers));
            ^noad;
        };

        desires = desirables['meter_beat -> Quarters'];
        // meter_beat -> Quarters
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Quarters", toker.burn(Toke_Quarters));
            ^noad;
        };

        desires = desirables['meter_beat -> Eighths'];
        // meter_beat -> Eighths
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Eighths", toker.burn(Toke_Eighths));
            ^noad;
        };

        desires = desirables['meter_beat -> Slash'];
        // meter_beat -> Slash
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Slash", toker.burn(Toke_Slash));
            ^noad;
        };

        // Error State
        this.fail;
    }

    optional_al_coda {
        | parent |

        var noad = SkoarNoad.new("optional_al_coda", nil, parent);
        var desires = nil;

        desires = desirables['optional_al_coda -> AlCoda'];
        // optional_al_coda -> AlCoda
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_AlCoda", toker.burn(Toke_AlCoda));
            ^noad;
        };

        // <e>
        ^noad;
    }

    clef {
        | parent |

        var noad = SkoarNoad.new("clef", nil, parent);
        var desires = nil;

        desires = desirables['clef -> TrebleClef'];
        // clef -> TrebleClef
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_TrebleClef", toker.burn(Toke_TrebleClef));
            ^noad;
        };

        desires = desirables['clef -> BassClef'];
        // clef -> BassClef
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_BassClef", toker.burn(Toke_BassClef));
            ^noad;
        };

        desires = desirables['clef -> AltoClef'];
        // clef -> AltoClef
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_AltoClef", toker.burn(Toke_AltoClef));
            ^noad;
        };

        // Error State
        this.fail;
    }

    msg {
        | parent |

        var noad = SkoarNoad.new("msg", nil, parent);
        var desires = nil;

        desires = desirables['msg -> MsgNameWithArgs listy_suffix'];
        // msg -> MsgNameWithArgs listy_suffix
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_MsgNameWithArgs", toker.burn(Toke_MsgNameWithArgs));
            this.listy_suffix(noad);
            ^noad;
        };

        desires = desirables['msg -> MsgName'];
        // msg -> MsgName
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_MsgName", toker.burn(Toke_MsgName));
            ^noad;
        };

        desires = desirables['msg -> listy'];
        // msg -> listy
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.listy(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    msg_chain_node {
        | parent |

        var noad = SkoarNoad.new("msg_chain_node", nil, parent);
        var desires = nil;

        desires = desirables['msg_chain_node -> optional_soak MsgOp msg msg_chain_node'];
        // msg_chain_node -> optional_soak MsgOp msg msg_chain_node
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.optional_soak(noad));
            noad.add_toke("Toke_MsgOp", toker.burn(Toke_MsgOp));
            noad.add_noad(this.msg(noad));
            noad.add_noad(this.msg_chain_node(noad));
            ^noad;
        };

        // <e>
        ^noad;
    }

    cthulhu_prime {
        | parent |

        var noad = parent;
        var desires = nil;

        desires = desirables['cthulhu_prime -> boolean CondSep RWing'];
        // cthulhu_prime -> boolean CondSep RWing
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.boolean(noad));
            noad.add_toke("Toke_CondSep", toker.burn(Toke_CondSep));
            noad.add_toke("Toke_RWing", toker.burn(Toke_RWing));
            ^noad;
        };

        desires = desirables['cthulhu_prime -> Nosey CondSep RWing'];
        // cthulhu_prime -> Nosey CondSep RWing
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Nosey", toker.burn(Toke_Nosey));
            noad.add_toke("Toke_CondSep", toker.burn(Toke_CondSep));
            noad.add_toke("Toke_RWing", toker.burn(Toke_RWing));
            ^noad;
        };

        // Error State
        this.fail;
    }

    assignment {
        | parent |

        var noad = SkoarNoad.new("assignment", nil, parent);
        var desires = nil;

        desires = desirables['assignment -> AssOp settable'];
        // assignment -> AssOp settable
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_AssOp", toker.burn(Toke_AssOp));
            this.settable(noad);
            ^noad;
        };

        // Error State
        this.fail;
    }

    listy {
        | parent |

        var noad = SkoarNoad.new("listy", nil, parent);
        var desires = nil;

        desires = desirables['listy -> ListS listy_suffix'];
        // listy -> ListS listy_suffix
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_ListS", toker.burn(Toke_ListS));
            this.listy_suffix(noad);
            ^noad;
        };

        // Error State
        this.fail;
    }

    listy_suffix {
        | parent |

        var noad = parent;
        var desires = nil;

        desires = desirables['listy_suffix -> listy_entries ListE'];
        // listy_suffix -> listy_entries ListE
        if (toker.sees(desires) != nil) {
            this.listy_entries(noad);
            noad.add_toke("Toke_ListE", toker.burn(Toke_ListE));
            ^noad;
        };

        // Error State
        this.fail;
    }

    meter_stmt_numbery {
        | parent |

        var noad = parent;
        var desires = nil;

        desires = desirables['meter_stmt_numbery -> meter_ass'];
        // meter_stmt_numbery -> meter_ass
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.meter_ass(noad));
            ^noad;
        };

        desires = desirables['meter_stmt_numbery -> meter_sig_prime'];
        // meter_stmt_numbery -> meter_sig_prime
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.meter_sig_prime(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    meteroid {
        | parent |

        var noad = SkoarNoad.new("meteroid", nil, parent);
        var desires = nil;

        desires = desirables['meteroid -> meter_symbolic'];
        // meteroid -> meter_symbolic
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.meter_symbolic(noad));
            ^noad;
        };

        desires = desirables['meteroid -> clef'];
        // meteroid -> clef
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.clef(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    nouny {
        | parent |

        var noad = SkoarNoad.new("nouny", nil, parent);
        var desires = nil;

        desires = desirables['nouny -> cthulhu'];
        // nouny -> cthulhu
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.cthulhu(noad));
            ^noad;
        };

        desires = desirables['nouny -> noaty'];
        // nouny -> noaty
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.noaty(noad));
            ^noad;
        };

        desires = desirables['nouny -> conditional'];
        // nouny -> conditional
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.conditional(noad));
            ^noad;
        };

        desires = desirables['nouny -> nouny_literal'];
        // nouny -> nouny_literal
        if (toker.sees(desires) != nil) {
            this.nouny_literal(noad);
            ^noad;
        };

        desires = desirables['nouny -> accidentally'];
        // nouny -> accidentally
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.accidentally(noad));
            ^noad;
        };

        desires = desirables['nouny -> musical_keyword'];
        // nouny -> musical_keyword
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.musical_keyword(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    marker {
        | parent |

        var noad = SkoarNoad.new("marker", nil, parent);
        var desires = nil;

        desires = desirables['marker -> Segno'];
        // marker -> Segno
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Segno", toker.burn(Toke_Segno));
            ^noad;
        };

        desires = desirables['marker -> Fine'];
        // marker -> Fine
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Fine", toker.burn(Toke_Fine));
            ^noad;
        };

        desires = desirables['marker -> coda'];
        // marker -> coda
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.coda(noad));
            ^noad;
        };

        desires = desirables['marker -> Volta'];
        // marker -> Volta
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Volta", toker.burn(Toke_Volta));
            ^noad;
        };

        desires = desirables['marker -> Bars'];
        // marker -> Bars
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Bars", toker.burn(Toke_Bars));
            ^noad;
        };

        // Error State
        this.fail;
    }

    optional_stmt {
        | parent |

        var noad = SkoarNoad.new("optional_stmt", nil, parent);
        var desires = nil;

        desires = desirables['optional_stmt -> stmt'];
        // optional_stmt -> stmt
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.stmt(noad));
            ^noad;
        };

        desires = desirables['optional_stmt -> Newline'];
        // optional_stmt -> Newline
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Newline", toker.burn(Toke_Newline));
            ^noad;
        };

        // <e>
        ^noad;
    }

    phrases {
        | parent |

        var noad = parent;
        var desires = nil;

        desires = desirables['phrases -> phrasey phrases'];
        // phrases -> phrasey phrases
        if (toker.sees(desires) != nil) {
            this.phrasey(noad);
            this.phrases(noad);
            ^noad;
        };

        // <e>
        ^noad;
    }

    noat_literal {
        | parent |

        var noad = SkoarNoad.new("noat_literal", nil, parent);
        var desires = nil;

        desires = desirables['noat_literal -> Choard'];
        // noat_literal -> Choard
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Choard", toker.burn(Toke_Choard));
            ^noad;
        };

        desires = desirables['noat_literal -> NamedNoat'];
        // noat_literal -> NamedNoat
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_NamedNoat", toker.burn(Toke_NamedNoat));
            ^noad;
        };

        // Error State
        this.fail;
    }

    musical_keyword {
        | parent |

        var noad = SkoarNoad.new("musical_keyword", nil, parent);
        var desires = nil;

        desires = desirables['musical_keyword -> dynamic'];
        // musical_keyword -> dynamic
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.dynamic(noad));
            ^noad;
        };

        desires = desirables['musical_keyword -> ottavas'];
        // musical_keyword -> ottavas
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.ottavas(noad));
            ^noad;
        };

        desires = desirables['musical_keyword -> pedally'];
        // musical_keyword -> pedally
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.pedally(noad));
            ^noad;
        };

        desires = desirables['musical_keyword -> musical_keyword_misc'];
        // musical_keyword -> musical_keyword_misc
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.musical_keyword_misc(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    meter_ass_r {
        | parent |

        var noad = parent;
        var desires = nil;

        desires = desirables['meter_ass_r -> Symbol'];
        // meter_ass_r -> Symbol
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Symbol", toker.burn(Toke_Symbol));
            ^noad;
        };

        desires = desirables['meter_ass_r -> meter_beat'];
        // meter_ass_r -> meter_beat
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.meter_beat(noad));
            ^noad;
        };

        desires = desirables['meter_ass_r -> dynamic'];
        // meter_ass_r -> dynamic
        if (toker.sees(desires) != nil) {
            noad.add_noad(this.dynamic(noad));
            ^noad;
        };

        // Error State
        this.fail;
    }

    meter_sig_prime {
        | parent |

        var noad = SkoarNoad.new("meter_sig_prime", nil, parent);
        var desires = nil;

        desires = desirables['meter_sig_prime -> Slash Int'];
        // meter_sig_prime -> Slash Int
        if (toker.sees(desires) != nil) {
            noad.add_toke("Toke_Slash", toker.burn(Toke_Slash));
            noad.add_toke("Toke_Int", toker.burn(Toke_Int));
            ^noad;
        };

        // Error State
        this.fail;
    }

}

